import { ProfitAndLossDirective } from './profit-and-loss.directive';

describe('ProfitAndLossDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfitAndLossDirective();
    expect(directive).toBeTruthy();
  });
});
